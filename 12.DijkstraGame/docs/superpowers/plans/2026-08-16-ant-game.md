# 《蚂蚁找食物》实现计划

**Goal:** Dijkstra 游戏完全重做——点亮点指挥蚂蚁 + 莽撞蚂蚁竞速对比，3 关递增。
**Architecture:** 系列同构：game.1.0.js 纯逻辑 TDD + ui.1.0.js SVG + 版本化文件名。

## Global Constraints
- 零依赖；全中文；大按钮；版本化文件名（style.1.0.css）
- 三关：4/6/8 路口；L2/L3 含莽撞陷阱（rashCost > optimalCost）
- 星级：0 错=3 / ≤2 错=2 / 其他=1
- 莽撞路线 = BFS 最少跳（并列取总步数小）；最优 = Dijkstra（暴力对拍）

### Task 1: game.js（TDD）
- API：`createAntGame(level)` → `{ nodes, edges, start, end, rashPath, getLight(), moveTo(id), getPanel(), getStats(), reset() }`
- 测试：三关结构/连通/≥2路径/陷阱存在/亮点顺序=moves序列/moveTo 对错/莽撞路径合法/暴力对拍最优/星级/reset
- commit

### Task 2: index.html + style.css（关卡选择 + SVG 路线图 + 木牌 + 亮点呼吸 + 蚂蚁动画 + 竞速结算）
- commit

### Task 3: ui.js（点亮点移动 + 松弛闪光 + 竞速对比 + 关卡推进 + 三关全通庆祝）
- Edge 冒烟 → commit

### Task 4: README + 门户卡片（蚂蚁找食物）+ 推送 + Pages 验证
- commit + push + 线上验证
