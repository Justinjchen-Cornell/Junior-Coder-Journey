# 《小猴子找朋友》实现计划

**Goal:** BFS 波纹扩散游戏——按按钮推圈，目标在第几圈=几步。
**Architecture:** 系列同构：game.1.0.js 纯逻辑 TDD + ui.1.0.js SVG 渲染 + 版本化文件名。

## Global Constraints
- 零依赖；全中文；大按钮；版本化文件名（style.1.0.css）
- 宝宝 6-8 节点目标 2-3 圈；挑战 12-16 节点目标 3-5 圈；图必须连通
- 星级：0 错=3 / ≤2 错=2 / 其他=1

### Task 1: game.js（TDD）
- API：`createBFSGame(mode)` → `{ expand(), guess(id), getStats(), reset(), isDone, foundLevel }`
- 测试：图连通/规模/目标不在起点/expand 层级不重复/目标层 found/guess 对错/完成后无效/星级/reset
- commit

### Task 2: index.html + style.css（SVG 图 + 扩散按钮 + 圈号徽章 + 波纹动画）
- commit

### Task 3: ui.js（渲染 + 扩散动画 + 揭示目标 + 结算）
- Edge 冒烟 → commit

### Task 4: README + 门户点亮 + 推送 + Pages 验证
- commit + push + 线上验证
